# Vision examples

```bash
python -m aion.vision.examples.demo_read_write
python -m aion.vision.examples.demo_transforms
python -m aion.vision.examples.demo_edges
```

Requires: `pip install 'aqwel-aion[vision]'`

# Sample TOML for aion.config examples (not loaded automatically).

[app]
name = "aion-demo"
debug = false

[db]
host = "localhost"
port = 5432
ssl = true

[llm]
provider = "openai"
timeout_seconds = 60

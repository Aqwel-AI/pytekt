# Vision examples

```bash
python -m pytekt.vision.examples.demo_read_write
python -m pytekt.vision.examples.demo_transforms
python -m pytekt.vision.examples.demo_edges
```

Requires: `pip install 'pytekt[vision]'`

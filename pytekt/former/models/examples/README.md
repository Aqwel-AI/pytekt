# pytekt.former.models — Examples

## Run

```bash
pip install -e ".[former]"
python -m pytekt.former.models.examples.demo_forward
```

| Script | Content |
|--------|---------|
| **demo_forward.py** | Tiny `Transformer`, one forward pass, logits shape `(batch, seq, vocab_size)`. |

Parent docs: [`../README.md`](../README.md)

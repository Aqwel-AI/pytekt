# pytekt.former.training — Examples

## Run

```bash
pip install -e ".[former]"
python -m pytekt.former.training.examples.demo_loss
```

| Script | Content |
|--------|---------|
| **demo_loss.py** | Random logits `Tensor`, integer targets, `cross_entropy_loss`, `backward()`. |

Parent docs: [`../README.md`](../README.md)

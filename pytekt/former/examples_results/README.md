# Example results

Outputs from `python -m pytekt.former.example` are saved here:

- `example_attention.png` — attention heatmap (layer 0, head 0)
- `example_training_metrics.png` — training loss curve
